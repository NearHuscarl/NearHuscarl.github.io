let projectName = (() => {
	$(document).ready(() => {
		// get copyright year
		$('.copyright__year').text(new Date().getFullYear());

		// press button to do stuff
		$('.search-bar__icon').on('click', () => {
		});

		$('.header__btn-all').on('click', () => {
		});
		$('.header__btn-online').on('click', () => {
		});
		$('.header__btn-offline').on('click', () => {
		});
	});
})();
